services:
  checkpoint:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
    # Environment variables are set in the Portainer UI (or a .env.production file for manual deploys)
